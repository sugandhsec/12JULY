#include <stdio.h>
#include <string.h>
int main()
{
    char name[200] = "Hello";
    char oname[20] = "Surfexcel ";
    // strcat(name,oname);
    // printf("name=%s oname=%s", name, oname);
    // printf("name=%s", strlwr(name));
    // printf("\nname=%s", name);
    //  printf("name=%s", strupr(name));
    // printf("\nname=%s", name);
    // strcpy(name, oname); // here value of oname gets copied to name variable and name variable old value gets deleted
    // strcpy(oname, name);
    // int a = strlen(name);
    // printf("Length=%d", strlen(name));
    return 0;
}
/*
strlen()---counts and returns the length of string
strcpy()
    strcpy(string1,string2)
    string2 value cpoise to string 1
strlwr()
strupr()
strcat()
strcmp()
*/