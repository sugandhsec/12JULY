#include <stdio.h>
int main()
{
    int num, sum = 0;
    for (int i = 1; i <= 5; i++)
    {
        printf("Enter number: ");
        scanf("%d", &num);
        sum = sum + num;
        // sum+=num;
    }
    printf("Addition =%d", sum);
    // printf("num * i = num*i");
    return 0;
}

printf("hehergherighi");
printf("you age is %d years old",age);