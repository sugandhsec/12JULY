#include <stdio.h>
int main()
{
    int num1, num2, add, sub, mul, mod, inc, dec;
    float div;
    printf("Enter number 1 amd number 2: ");
    scanf("%d %d", &num1, &num2);
    // add = num1 + num2;
    // sub = num1 - num2;
    // mul = num1 * num2;
    // div = (float)num1 / (float)num2;
    // mod = num1 % num2;
    // inc = ++num1;
    // dec = --num2;
    // printf("addition answer is %d\n", add);
    // printf("sub answer is %d\n", sub);
    // printf("div answer is %f\n", div);
    // printf("mul answer is %d\n", mul);
    // printf("modulus answer is %d\n", mod);
    // printf("increment answer is %d\n", inc);
    // printf("decrement answer is %d\n", dec);
    // printf("increment of somenew %d", ++num2);
    // printf("increment of somenew %d", num2);
K
    return 0;
}