#include<stdio.h>
int main()
{
    float per;
    printf("Enter Percentage to know what your grade is: ");
    scanf("%f",&per);
    if(per>=91 && per<=100)
    {
        printf("Your grade is A1");
    }
    else if(per>=81 && per<=90)
    {
        printf("Your grade is A1");
    }
    else if(per>=71 && per<=80)
    {
        printf("Your grade is A1");
    }
    else if(per>=61 && per<=70)
    {
        printf("Your grade is A1");
    }
    else if(per>=51 && per<=60)
    {
        printf("Your grade is A1");
    }
    else if(per>=41 && per<=50)
    {
        printf("Your grade is A1");
    }
    else if(per>=33 && per<=40)
    {
        printf("Your grade is A1");
    }
    return 0;
}